---
declared-services:
  tone-analyzer-service:
    label: tone_analyzer
    plan: beta
applications:
- services:
  - tone-analyzer-svc
  name: watson-elections
  command: npm start
  path: .
  instances: 1
  memory: 128MB
